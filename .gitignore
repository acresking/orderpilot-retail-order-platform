node_modules/
.DS_Store
# Local/private data and secrets — this repo is a reusable template + the generator tool, never
# a live deployment. Real client data/keys live in ../clients/, outside version control entirely.
data/
.env
.env.*
!.env.example
generator/data/
generator/exports/
# Dependencies and generated apps
android/.gradle/
android/app/build/
ios/App/Pods/
ios/App/build/
# Build outputs
dist-installers/
dist-desktop/
dist-server/
out/
release/
build/
# Temporary patch files
patch_v*.py
*.bak-*
# Logs
*.log
npm-debug.log*
android/local.properties
