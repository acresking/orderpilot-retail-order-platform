node_modules/
.DS_Store
# Local/private data and secrets — this is a reusable template, never a live deployment. Real
# client data/keys live in a generated client's own folder, outside version control entirely.
data/
.env
.env.*
!.env.example
# Dependencies and generated apps
android/.gradle/
android/app/build/
android/local.properties
ios/App/Pods/
ios/App/build/
# Build outputs
dist-installers/
dist-desktop/
dist-server/
out/
release/
build/
exports/
# Temporary patch files
patch_v*.py
*.bak-*
# Logs
*.log
npm-debug.log*
